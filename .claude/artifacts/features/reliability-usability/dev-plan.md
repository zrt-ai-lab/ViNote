# 实施计划

1. 无损分段、有界摘要、全文卡片材料、多来源召回；用纯函数测试。
2. 按笔记协调删除/重生成，先生成暂存再提交，异常回滚；不承诺断电级跨 SQLite/文件系统事务。
3. 增量文件名契约与 SQLite 内容索引/回填；保持旧响应兼容。
4. 前端恢复只存 ID；会话列表和确认删除；验证正常/失败/恢复。
5. 可选依赖、启动缓存、Windows call、回归 CI。

验收：隔离 unittest、发布静态检查、前端 lint/build、本地启动与浏览器；真实模型/ASR、Windows 实机分开说明。
