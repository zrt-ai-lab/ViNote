# 前端变更记录

## 2026-09-05：跨平台轮询测试同步

轮询回归改为等待实际完成状态（最多 2 秒），退出时清理观察器；保留重试次数、结果及无重叠断言，避免 Windows 定时精度造成固定 35 毫秒等待提前结束。仅调整测试，不改变运行时行为。

## 2026-09-05：恢复任务、继续会话与正确下载

修复笔记/摘要文件名错配，下载失败可见；保存不含内容的任务 ID 并恢复进度，断线使用串行轮询。问答页增加最近会话与确认删除，离开会话时终止旧流；历史检索提示覆盖全文，清理部分失败明确保留条数。

影响传播：真实文件名 → TaskStatus → VideoNote 下载；共享下载与流式客户端 → SearchAgent / VideoQA；任务观察器 → VideoNote 单任务/批次和 VideoQA 预处理。保留原有 React、组件和 API 路由，不新增运行时依赖。

回归入口：`npm test`、`npm run lint`、`npm run build`，以及统一全栈浏览器验收。
